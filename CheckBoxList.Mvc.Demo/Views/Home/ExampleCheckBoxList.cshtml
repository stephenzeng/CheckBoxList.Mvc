@using System.Web.Mvc.Html
@using CheckBoxList.Mvc.Html
@model IList<CheckBoxList.Mvc.CheckBoxListItem>

@{
    Layout = "~/Views/Shared/_ExampleLayout.cshtml";
}

<h3 class="page-header">
    <b>Example - @Html.Encode("@Html.CheckBoxList()")</b>
</h3>

@using (Html.BeginForm())
{
    <div class="form-group">
        <label>Please choose your investment preferences listed below.</label>
        <div style="margin-top: 15px;">
            @Html.CheckBoxList("InvestmentOptions", Model)
        </div>
    </div>

    <div style="margin: 35px 0;">
        <button type="submit" class="btn btn-default">Submit</button>
    </div>
}

@if (!string.IsNullOrEmpty(ViewBag.SelectedOptionsText))
{
    <div class="alert alert-info">
        Your have selected:
        <b>@ViewBag.SelectedOptionsText.</b>
    </div>
}